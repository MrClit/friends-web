# Neon + Render Deploy Runbook (Backend)

This document is superseded by the canonical deployment guide:

- [../../DEPLOYMENT.md](../../DEPLOYMENT.md)

Reason:

- Deployment instructions are now centralized in a single stable location outside `docs/`.
- This file is kept only as a historical reference.
